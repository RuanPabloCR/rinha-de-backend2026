services:
  nginx:
    image: nginx:alpine
    container_name: nginx-lb
    platform: linux/amd64
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api1
      - api2
    deploy:
      resources:
        limits:
          cpus: "0.3"
          memory: 36M
    restart: unless-stopped
    networks:
      - backend

  api1: &api-template
    image: ruanpcr/rinha-backend2026:latest
    platform: linux/amd64
    deploy:
      resources:
        limits:
          cpus: "0.35" 
          memory: 157M
    restart: unless-stopped
    networks:
      - backend

  api2:
    <<: *api-template

networks:
  backend:
    driver: bridge